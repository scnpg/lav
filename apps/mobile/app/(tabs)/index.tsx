import { PlaceholderScreen } from "../../src/components/PlaceholderScreen";

export default function MapScreen() {
  return (
    <PlaceholderScreen
      title="Map"
      description="Nearby bathrooms will show up here once MapLibre is wired in. Search, filters, and the 'use my location' button all land with that."
    />
  );
}
