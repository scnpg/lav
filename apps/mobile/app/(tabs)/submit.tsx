import { PlaceholderScreen } from "../../src/components/PlaceholderScreen";

export default function SubmitScreen() {
  return (
    <PlaceholderScreen
      title="Submit a bathroom"
      description="The submission form lands here - name, location, access type, photos, and the rest. Every submission goes through moderation before it's public."
    />
  );
}
